package com.ssm.util;

import com.ssm.entity.bo.MailTemplate;

public class EMailUtil {
	
	public static void sendMail(MailTemplate mailTemplate)
	{
		//INotificationManager notificationManager = (INotificationManager)ServiceLocator.getInstance().getBeanFactory().getBean("notificationManagerImpl");
		//notificationManager.sendMail(mailTemplate);
	}
	
}
